<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="robots" content="noindex, nofollow">
  <meta name="description" content="
    {%- if description %}
      {{ description }}
    {% else %}
      {{ site.description }}
    {% endif %}">
  {% set brand_color = "#f5ce0a" %}
  <meta name="theme-color" content="{{ brand_color }}">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="apple-mobile-web-app-status-bar-style" content="{{ brand_color }}">
  <meta name="msapplication-navbutton-color" content="{{ brand_color }}">
  <title>{% if title %} {{ title }} | {% endif %} {{ site.name }}</title>
  <link rel="stylesheet" href="{{ '/css/main.css' | url }}">
  <link rel="apple-touch-icon" sizes="180x180" href="{{ '/assets/img/favicon/apple-touch-icon.png' | url }}">
  <link rel="icon" type="image/png" sizes="32x32" href="{{ '/assets/img/favicon/favicon-32x32.png' | url }}">
  <link rel="icon" type="image/png" sizes="16x16" href="{{ '/assets/img/favicon/favicon-16x16.png' | url }}">
</head>
<body>

  {% include 'header.njk' %}

  <main>
    {{ content | safe }}
  </main>

  {% include 'footer.njk' %}

  <script src="{{ '/js/main.js' | url }}" defer></script>
  
</body>
</html>